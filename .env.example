# Met Office DataPoint API key (optional)
# Register free at https://datahub.metoffice.gov.uk/
# If not set, the pipeline will use Playwright to scrape the Met Office website instead.
METOFFICE_API_KEY=
